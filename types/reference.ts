export type Region = { id: string; name: string; peakSunHours: number };
export type PanelSpec = { id: string; watts: number };
export type BatteryType = { id: string; name: string; dod: number };
export type RoofOrientation = { id: string; name: string; factor: number };
